<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.treemarket.tree.mapper.UserMapper">
    <!-- 유저 리스트 -->
    <select id="findAllUser" resultType="com.treemarket.tree.domain.UserVO">
        SELECT * FROM usertbl
    </select>

    <insert id="saveUser" parameterType="com.treemarket.tree.domain.UserVO">
        INSERT INTO usertbl VALUES
            (USERTBL_SEQ.NEXTVAL, #{userId}, #{userAddress}, #{userPw}, #{userName},
             #{userNickname},#{userPhoneno}, 1)
    </insert>

    <select id="getUserId" resultType="com.treemarket.tree.domain.UserVO">
        SELECT * FROM usertbl WHERE userid = #{userId}
    </select>

    <select id="getUserNickName" resultType="com.treemarket.tree.domain.UserVO">
        SELECT * FROM usertbl WHERE usernickname = #{userNickname}
    </select>


    <update id="updateUser" parameterType="com.treemarket.tree.domain.UserVO">
        UPDATE usertbl
        SET userID = #{userID}, userAddress = #{userAddress}, userPW = #{userPW},
            userName = #{userName}, userNickName = #{userNickName},
            userPhoneNo = #{userPhoneNo}, userValidity = #{userValidity}
        WHERE userNo = #{userNo}
    </update>




</mapper>
